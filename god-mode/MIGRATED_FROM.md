# Migrated from standalone repo

This project was moved from the standalone repository path:

- `~/GitProjects/AndroidGodMode`

It is now managed as a subproject inside:

- `~/GitProjects/MC_AndroidApp/god-mode`
